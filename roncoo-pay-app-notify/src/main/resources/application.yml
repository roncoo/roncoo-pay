server:
  port: 8086
  servlet:
    session:
      timeout: 30
spring:
  application:
    name: roncoo-pay-spring-boot-app-notify
logging:
  config: classpath:logback.xml
mybatis:
  mapper-locations: classpath*:mybatis/mapper/**/*.xml